printf("%d %d\n", v.row[0], v.counter);
        printf("Row Array: ");
        for(i = 0; i < SIZE; i++)
        {
            printf("%d ", v.row[i]);
        }
        printf("\n");
        printf("Thread Array: ");
        for(i = 0; i < SIZE; i++)
        {
            printf("%d ", v.sub[i]);
        }
        printf("\n");
        printf("Col Array: ");
        for(i = 0; i < SIZE; i++)
        {
            printf("%d ", v.col[i]);
        }
        printf("\n");